{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "env": {
    "CI": "true"
  },
  "routes": [
    {
      "src": "^/(?!.*\\.).*$",
      "destination": "/index.html"
    }
  ]
}
